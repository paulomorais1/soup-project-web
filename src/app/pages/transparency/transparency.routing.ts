import { type Routes } from '@angular/router';

import { TransparencyComponent } from '@pages/transparency/transparency.component';

export const TransparencyRouting: Routes = [
  {
    path: '',
    component: TransparencyComponent
  }
];
